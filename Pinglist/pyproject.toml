[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-pinglist"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
Pinglist = "limnoria_pinglist"

[tool.setuptools.package-dir]
"limnoria_pinglist" = "."
"limnoria_pinglist.local" = "local/"

[tool.setuptools.package-data]
"limnoria_pinglist" = ["locales/*.po"]

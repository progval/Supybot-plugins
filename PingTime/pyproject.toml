[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-pingtime"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
PingTime = "limnoria_pingtime"

[tool.setuptools.package-dir]
"limnoria_pingtime" = "."
"limnoria_pingtime.local" = "local/"

[tool.setuptools.package-data]
"limnoria_pingtime" = ["locales/*.po"]

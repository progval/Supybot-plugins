[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-ratelimit"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
RateLimit = "limnoria_ratelimit"

[tool.setuptools.package-dir]
"limnoria_ratelimit" = "."
"limnoria_ratelimit.local" = "local/"

[tool.setuptools.package-data]
"limnoria_ratelimit" = ["locales/*.po"]

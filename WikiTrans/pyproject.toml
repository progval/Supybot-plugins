[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-wikitrans"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
WikiTrans = "limnoria_wikitrans"

[tool.setuptools.package-dir]
"limnoria_wikitrans" = "."
"limnoria_wikitrans.local" = "local/"

[tool.setuptools.package-data]
"limnoria_wikitrans" = ["locales/*.po"]

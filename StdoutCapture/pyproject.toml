[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-stdoutcapture"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
StdoutCapture = "limnoria_stdoutcapture"

[tool.setuptools.package-dir]
"limnoria_stdoutcapture" = "."
"limnoria_stdoutcapture.local" = "local/"

[tool.setuptools.package-data]
"limnoria_stdoutcapture" = ["locales/*.po"]

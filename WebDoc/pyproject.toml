[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-webdoc"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
WebDoc = "limnoria_webdoc"

[tool.setuptools.package-dir]
"limnoria_webdoc" = "."
"limnoria_webdoc.local" = "local/"

[tool.setuptools.package-data]
"limnoria_webdoc" = ["locales/*.po"]

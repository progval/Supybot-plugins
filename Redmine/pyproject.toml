[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-redmine"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
Redmine = "limnoria_redmine"

[tool.setuptools.package-dir]
"limnoria_redmine" = "."
"limnoria_redmine.local" = "local/"

[tool.setuptools.package-data]
"limnoria_redmine" = ["locales/*.po"]

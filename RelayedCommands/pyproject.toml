[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-relayedcommands"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
RelayedCommands = "limnoria_relayedcommands"

[tool.setuptools.package-dir]
"limnoria_relayedcommands" = "."
"limnoria_relayedcommands.local" = "local/"

[tool.setuptools.package-data]
"limnoria_relayedcommands" = ["locales/*.po"]

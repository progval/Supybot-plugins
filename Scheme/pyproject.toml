[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-scheme"
version = "0.0.0"
authors = [
    { name = "Valentin Lorentz" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
Scheme = "limnoria_scheme"

[tool.setuptools.package-dir]
"limnoria_scheme" = "."
"limnoria_scheme.local" = "local/"

[tool.setuptools.package-data]
"limnoria_scheme" = ["locales/*.po"]
